<% if status == "new" %>
  <div id="star_comment_new"></div>
    <script>
      var elem = document.querySelector('#star_comment_new');
      elem.innerHTML = ""
      var option = {
        starOn: "<%= asset_path('star-on.png') %>",
        starOff: "<%= asset_path('star-off.png') %>",
        scoreName: "comment[good]",
        score: <%= 0 %>
      };
      raty(elem, option);
    </script>
<% elsif status == "show" %>
<div id="star_comment_show_<%= commentdata.id %>"></div>
  <script>
    var elem = document.querySelector("#star_comment_show_<%= commentdata.id %>");
    elem.innerHTML = ""
    var option = {
      starOn: "<%= asset_path('star-on.png') %>",
      starOff: "<%= asset_path('star-off.png') %>",
      readOnly: true,
      <% if commentdata.good.present? %>
          score: <%= commentdata.good %>
        <% else %>
          score: 0
      <% end %>
    };
  raty(elem, option);
  </script>
<% end %>